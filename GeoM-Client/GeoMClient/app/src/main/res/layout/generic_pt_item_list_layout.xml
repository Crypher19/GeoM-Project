<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    android:focusable="true"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:clickable="true"
    android:background="?android:attr/selectableItemBackground"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/pt_image"
        android:src="@drawable/ic_material_no_image_grey"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/pt_info"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignTop="@+id/pt_type" />

    <TextView
        android:id="@+id/pt_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/pt_image"
        android:layout_toEndOf="@+id/pt_image"
        android:layout_alignParentTop="true"
        android:textSize="30sp"
        android:text="ciaoooo"
    />

    <TextView
        android:id="@+id/pt_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/pt_image"
        android:layout_toEndOf="@+id/pt_image"
        android:layout_below="@+id/pt_type"
        android:text="ciaoooo"
    />

</RelativeLayout>