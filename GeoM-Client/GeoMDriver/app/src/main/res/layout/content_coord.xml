<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".CoordActivity"
    tools:showIn="@layout/activity_coord">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Coordinate"
        android:id="@+id/textViewCoord"
        android:layout_marginTop="80dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/ptImageView"
        android:src="@drawable/ic_launcher"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="29dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nome utente"
        android:id="@+id/usernameTextView"
        android:paddingLeft="20dp"
        android:paddingStart="20dp"
        android:layout_alignTop="@+id/ptImageView"
        android:layout_toRightOf="@+id/ptImageView"
        android:layout_toEndOf="@+id/ptImageView" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mezzo guidato"
        android:id="@+id/ptTextView"
        android:paddingLeft="20dp"
        android:paddingStart="20dp"
        android:layout_alignBottom="@+id/ptImageView"
        android:layout_toRightOf="@+id/ptImageView"
        android:layout_toEndOf="@+id/ptImageView" />

</RelativeLayout>
